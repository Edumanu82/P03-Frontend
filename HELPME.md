* What went wrong:
Error resolving plugin [id: 'com.facebook.react.settings']
> 25

Command: export JAVA_HOME=$(/usr/libexec/java_home -v 21)

* What went wrong:
A problem occurred evaluating root project 'p03-frontend'.
> Failed to apply plugin 'com.facebook.react.rootproject'.
   > A problem occurred configuring project ':app'.
      > com.android.builder.errors.EvalIssueException: SDK location not found. Define a valid SDK location with an ANDROID_HOME environment variable or by setting the sdk.dir path in your project's local properties file at

Command: export ANDROID_HOME=$HOME/Library/Android/sdk
